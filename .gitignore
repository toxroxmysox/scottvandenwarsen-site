# Hugo build output
/public/
/resources/
.hugo_build.lock

# OS junk
.DS_Store

# Editor junk
.vscode/
.idea/

# Claude Code worktrees (ephemeral, per-session)
.claude/worktrees/

# iOS Shortcuts (contain credentials — never commit)
shortcuts/*.cherri
shortcuts/*.plist
shortcuts/*.shortcut
shortcuts/*.xml
shortcuts/*_processed.cherri
